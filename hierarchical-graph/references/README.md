# Reference fixtures

`python-agent-app/` pairs a synthetic Python repository with its expected hierarchical graph. It demonstrates a repository-to-component-to-module-to-significant-class hierarchy while retaining an ordinary data class as symbol metadata.

